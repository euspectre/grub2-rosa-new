diff -Nur grub-2.00.old/util/grub.d/10_linux.in grub-2.00/util/grub.d/10_linux.in
--- grub-2.00.old/util/grub.d/10_linux.in	2012-12-04 16:42:17.963280141 +0400
+++ grub-2.00/util/grub.d/10_linux.in	2012-12-04 16:37:52.000000000 +0400
@@ -28,10 +28,10 @@
 
 CLASS="--class gnu-linux --class gnu --class os"
 
-if [ "x${GRUB_DISTRIBUTOR}" = "x" ] ; then
-  OS="$(sed 's, release .*$,,g' /etc/system-release) Linux"
+if [ "x${GRUB_DISTRIBUTOR}" = "xROSA" ] ; then
+  OS="$(sed 's, release .*$,,g' /etc/system-release) 2012"
 else
-  OS="${GRUB_DISTRIBUTOR} Linux"
+  OS="${GRUB_DISTRIBUTOR}"
   CLASS="--class $(echo ${GRUB_DISTRIBUTOR} | tr 'A-Z' 'a-z' | cut -d' ' -f1) ${CLASS}"
 fi
 
